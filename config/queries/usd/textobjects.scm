(prim_definition
  (block) @class.inner) @class.outer

(variant_set_definition
  (variant) @parameter.inner) @parameter.outer

(attribute_declaration) @parameter.outer

(relationship_declaration) @parameter.outer

(metadata) @block.inner

(comment) @comment.outer
