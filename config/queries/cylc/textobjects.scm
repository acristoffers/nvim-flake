; Top-level [section] headers
(top_section) @class.outer

; [[task]] definitions under [runtime]
(task_section) @function.outer

; Other nested sections / blocks
[
  (runtime_section)
  (sub_section_1)
  (sub_section_2)
  (graph_section)
] @block.outer

(comment) @comment.outer
