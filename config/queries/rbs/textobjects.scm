[
  (class_decl)
  (module_decl)
  (interface_decl)
] @class.outer

(method_member) @function.outer
(attribute_member) @function.outer

(parameter) @parameter.outer @parameter.inner
(unnamed_parameter) @parameter.outer @parameter.inner

(comment) @comment.outer
