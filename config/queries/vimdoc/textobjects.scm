(block
  .
  (line (h1))
  .
  (_)+ @section.inner
  .) @section.outer

(block
  .
  (line (h2))
  .
  (_)+ @section.inner
  .) @section.outer

(block
  .
  (line (h3))
  .
  (_)+ @section.inner
  .) @section.outer

(codeblock
  (code) @block.inner) @block.outer

(tag) @parameter.outer

(argument) @parameter.outer
