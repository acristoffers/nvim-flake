; Ingredients / cookware / timers as parameter-like objects
(ingredient
  (amount) @parameter.inner) @parameter.outer

(cookware
  (amount) @parameter.inner) @parameter.outer

(timer
  (amount) @parameter.inner) @parameter.outer

(comment) @comment.outer
