return {
  settings = {
    yaml = {
      hover = true,
      -- format = {
      --   enable = true,
      --   singleQuote = true,
      -- },
      completion = true,
      validate = true,
      schemaStore = {
        enable = true,
      },
      customTags = {
        "!reference sequence"
      },
    },
  }
}
